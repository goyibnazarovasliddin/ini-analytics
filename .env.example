# ==============================================================================
# FRONTEND CONFIGURATION (Vite)
# ==============================================================================

# The base URL for the backend API. 
# Defaults to localhost:8000/api/v1 for local development.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Admin Key required for triggering data refreshes.
# Must match the ADMIN_KEY defined in the backend .env.
VITE_ADMIN_KEY=super-secret-admin-key

# App Display Name (Visual only)
VITE_APP_NAME="INI Analytics - Uzbekistan"

# DEFAULT LANGUAGE (uz | ru | en)
VITE_DEFAULT_LANG=uz

# DEFAULT THEME (dark | light)
VITE_THEME=dark


# ==============================================================================
# BACKEND CONFIGURATION
# ==============================================================================
# Note: These values should be placed in backend/.env for production, 
# but are listed here for overview.

# Server Port
# PORT=8000

# Prisma Database URL
# For SQLite: "file:./dev.db"
# For PostgreSQL: "postgresql://user:password@localhost:5432/dbname"
# DATABASE_URL="file:./dev.db"

# Admin Job Key (Must match Frontend VITE_ADMIN_KEY)
# ADMIN_KEY="super-secret-admin-key"

# Source SDMX Excel URL (Default: Official Uzbekistan Price Index)
# SOURCE_URL="https://api.siat.stat.uz/media/uploads/sdmx/sdmx_data_4585.xlsx"

# Allowed CORS Origins (Comma separated)
# CORS_ORIGINS="http://localhost:5173,http://localhost:3000"
